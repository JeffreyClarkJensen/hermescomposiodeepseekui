# Jarvis Composio Proxy — Railway deployment
# Set these in Railway dashboard (not in .env committed to git)

# Port (Railway sets this automatically)
PORT=3333

# Hermes backend API URL
HERMES_API_URL=https://hermes-api-production-34cb.up.railway.app

# Composio MCP endpoint
COMPOSIO_MCP_URL=https://connect.composio.dev/mcp
